import { LoadingSpinner, LoadingSpinnerContainer } from "./loading.styles";

function Loading() {
  return (
    <LoadingSpinnerContainer>
      <LoadingSpinner>     
      </LoadingSpinner>
    </LoadingSpinnerContainer>
  );
}

export default Loading;
